language: ruby
rvm:
- 2.3

before_script:
 - chmod +x travis.sh

before_install:
 - gem update --system
 - gem install bundler

script: ./travis.sh

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

branches:
  only:
    - master
